# Pinned to what this repo is actually developed/tested against - an
# unpinned pipecat-ai/fastapi in particular pull in breaking API changes
# often enough that "pip install -r requirements.txt" six months from now
# was not reliably reproducing a working environment.
fastapi==0.141.1
uvicorn[standard]==0.52.0
requests==2.34.2
python-dotenv==1.2.2
pipecat-ai[openai,silero,webrtc]==1.6.0
websockets==17.0.1
pytest==9.1.1
ruff==0.16.6
